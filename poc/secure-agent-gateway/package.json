{
  "name": "secure-agent-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Proof-of-concept: deterministic security gateway for an LLM agent — the model proposes, the platform validates, authorizes, confirms, executes, and audits.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:redteam": "vitest run tests/redteam"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
